#!/usr/bin/env bash

EXE="./target/debug/ece461_team19_cli"

if [[ "$1" == "build" ]]; then
    cargo build
elif [[ "$1" == "install" ]]; then
    echo "install what"
elif [[ "$1" == "test" ]]; then
    echo test
else
    echo url
fi
